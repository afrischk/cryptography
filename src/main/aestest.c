#include "aes.h"
#include "aes_s_box.h"
#include <stdlib.h>

int main(void) {
  char const *key = "YELLOW SUBMARINE";
  // word *w = malloc(sizeof(word) * AES_BLOCK_SIZE * (AES_128_NUM_OF_ROUNDS +
  // 1)); aes_expand_key(key, w, AES_128_KEY_LEN, AES_128_NUM_OF_ROUNDS);
}
